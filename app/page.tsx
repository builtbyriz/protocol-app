export default function Home() {
    return (
        <main style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
            <h1>Hello from Fresh Start (OpenNext)</h1>
            <p>If you can see this, the OpenNext build pipeline is working correctly.</p>
            <p>Branch: fresh-start</p>
        </main>
    );
}
